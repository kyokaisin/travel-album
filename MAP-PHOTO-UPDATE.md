# 底图和照片压缩更新

## 更新方法
先备份本机草稿。解压更新包，将里面的所有文件上传到原仓库最外层并提交，覆盖同名文件。等 Pages 部署完成后刷新 Safari。
包内没有 album.json、私人备份和照片，不会替换你的旅行数据；保留原仓库里的其他文件。

## 新底图
改用 OpenFreeMap Positron 浅色矢量底图，国界层的样式明确排除了 maritime=1 的海上边界，去掉原来海岸与岛礁周围的圆圈。绿色站点和旅行路线保留。
地图数据、文字和图标来自 OpenFreeMap 在线服务，不需要在网站填写地图 API 密钥。程序依赖保存在仓库中。
地图需要浏览器支持 WebGL。底图下载失败时显示提示，地点列表和照片仍可用。在线底图服务会接收网络请求信息。
样式入口：map-style.json。图库来源和许可证见包内 LICENSE 文件。该样式的 Positron 地图设计源自 CARTO。

## 照片自动压缩
在「整理相册」里选择照片时，先在设备内处理，再存进本机草稿；导出并上传 GitHub 的是处理后的版本。
- 单张上限 500,000 字节（500 KB）。
- 最长边先限制为 1800 像素，不放大小照片。
- 调整 JPEG 质量；仍超限时继续缩小尺寸。
- 输出重新编码的 JPEG，不保留原文件名和原图 EXIF。
- 不修改设备里的原始照片。
- 小照片可能只有几十 KB，无需凑到 500 KB。
- 已在草稿或网站里的旧照片不会自动重新压缩。重新选择原图才会应用新规则。
- 直接使用 GitHub 上传原图，不会经过这段压缩流程。
- 此次没有解除原编辑器合计 98 张公开照片的导出限制。

压缩代码：compression.mjs。PHOTO_MAX_BYTES 可调整体积上限；preparePhoto 在 storage.js 里调用。
验证：使用真实 JPEG 编码测试了风景照片、高细节噪声图、小图，输出均小于 500 KB；尚未完成 iPad 实机地图与压缩测试。

参考：https://openfreemap.org/quick_start/
